version: '3.8'

services:
  # OCR 분석 및 작업 자동화 엔진
  n8n:
    image: docker.n8n.io/n8nio/n8n:latest
    container_name: n8n
    restart: always
    ports:
      - "127.0.0.1:5678:5678"
    environment:
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678/
    volumes:
      - "${N8N_DATA_PATH:-C:/n8n_data}:/home/node/.n8n"
    networks:
      - app-network

  # 통합 CI/CD 빌드 및 배포 제어기
  jenkins:
    build:
      context: .
      dockerfile: Dockerfile.jenkins
    container_name: jenkins
    user: root
    restart: always
    ports:
      - "9090:8080"
      - "50000:50000"
    environment:
      - ANDROID_HOME=/var/jenkins_home/android-sdk
    volumes:
      - jenkins_home:/var/jenkins_home
      - "${DEPLOY_DIR:-C:/yakallim-deploy}:/var/jenkins_home/deploy"
      - "${ANDROID_SDK_PATH:-C:/Users/a4336/AppData/Local/Android/Sdk}:/var/jenkins_home/android-sdk"
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - app-network

# 서비스 간 격리 가상 네트워크
networks:
  app-network:
    name: app-network
    driver: bridge

# 영속성 상태 관리 데이터 스토리지
volumes:
  jenkins_home:
    external: true
